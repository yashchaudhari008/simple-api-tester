{
  "name": "simple-api-tester",
  "homepage": "https://yashchaudhari008.github.io/simple-api-tester",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "predeploy": "vite build",
    "deploy": "gh-pages -d dist",
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview",
    "backend": "json-server --watch db.json --port 3000"
  },
  "dependencies": {
    "json-server": "^0.17.1",
    "vue": "^3.2.45"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.0.0",
    "gh-pages": "^5.0.0",
    "vite": "^4.0.0"
  }
}
